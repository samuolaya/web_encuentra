# shared

Tipos y utilidades transversales a todos los dominios. Sin lógica de un solo
área. Sin dependencias de capas externas.

Ejemplo: `result.ts` (envoltura de éxito/error y forma de respuesta paginada
del backend para los `list`).
