# domain / search

Cotejo facial: subir una imagen y recibir coincidencias ordenadas.

- `match-result.model.ts` — `MatchResult` (persona + similitud/distancia) y
  `SearchByImageInput` (imagen cargada + cédula del solicitante).
- `search.repository.ts` — interfaz `SearchRepository`.
